<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SearchAndBook.Views.Controls.GameCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SearchAndBook.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image
        Source="{Binding ImageUrl, RelativeSource={RelativeSource Mode=TemplatedParent}}"
        Height="120"
        Width="140"
        Stretch="UniformToFill"/>

        <StackPanel Grid.Column="1" Spacing="6">

            <TextBlock
            Text="{Binding Title, RelativeSource={RelativeSource Mode=TemplatedParent}}"
            FontSize="18"
            FontWeight="SemiBold"/>

            <TextBlock
            Text="{Binding Location, RelativeSource={RelativeSource Mode=TemplatedParent}}"
            FontSize="14"/>

            <TextBlock
            Text="{Binding PlayersText, RelativeSource={RelativeSource Mode=TemplatedParent}}"
            FontSize="14"/>

            <TextBlock
            Text="{Binding PriceText, RelativeSource={RelativeSource Mode=TemplatedParent}}"
            FontSize="14"
            FontWeight="Medium"/>

        </StackPanel>
    </Grid>
</UserControl>
